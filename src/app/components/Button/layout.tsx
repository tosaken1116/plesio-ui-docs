import type { ReactNode } from 'react';

export const metadata = {
  title: 'Button Component',
  description: 'Plesiosaurus UI Button Component',
};

const Layout = ({ children }: { children: React.ReactNode }): ReactNode =>
  children;

export default Layout;
